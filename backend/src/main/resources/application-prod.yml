spring:
  datasource:
    url: jdbc:mysql://localhost:3306/drools_db_prod?useUnicode=true&characterEncoding=utf8&useSSL=false&serverTimezone=Asia/Shanghai
    username: ${DB_USERNAME:root}
    password: ${DB_PASSWORD:password}

logging:
  level:
    com.example.drools: INFO
    org.drools: WARN